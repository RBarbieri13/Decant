# ============================================================
# Decant Environment Configuration
# ============================================================

# Server Configuration
# ============================================================

# Port to run the server on (1-65535)
PORT=3000

# Environment mode: development, production, or test
NODE_ENV=development

# Database Configuration
# ============================================================

# Path to SQLite database file
DATABASE_PATH=./data/decant.db

# Rate Limiting Configuration
# ============================================================

# Global rate limit window (milliseconds)
# Default: 60000 (1 minute)
RATE_LIMIT_WINDOW_MS=60000

# Maximum requests per window for general API endpoints
# Default: 100
RATE_LIMIT_MAX=100

# Maximum import/AI requests per window
# Import operations use AI classification which is resource-intensive
# Default: 10
RATE_LIMIT_IMPORT_MAX=10

# Maximum settings requests per window
# Settings endpoints are more sensitive
# Default: 5
RATE_LIMIT_SETTINGS_MAX=5

# CORS Configuration
# ============================================================

# Comma-separated list of allowed origins
# Default: http://localhost:3000,http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Security Configuration
# ============================================================

# Master key for encrypting API keys stored in database (min 32 characters)
# Generate with: openssl rand -base64 32
# DECANT_MASTER_KEY=your-32-character-minimum-secret-key

# OpenAI API Configuration
# ============================================================

# Your OpenAI API key (required for AI import features)
# Get your key from https://platform.openai.com/api-keys
# Remove the # to uncomment and add your key
# OPENAI_API_KEY=sk-proj-your-key-here

# OpenAI model to use for AI classification
# Default: gpt-4o-mini
# Options: gpt-4o-mini, gpt-4o, gpt-3.5-turbo, etc.
OPENAI_MODEL=gpt-4o-mini

# Logging Configuration
# ============================================================

# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Enable pretty logging (set to false for production JSON logs)
LOG_PRETTY=true

# Log format: json or pretty (overrides LOG_PRETTY if set)
# LOG_FORMAT=pretty

# Scraper Configuration
# ============================================================

# Timeout for URL scraping operations (milliseconds, 1000-120000)
# Default: 30000 (30 seconds)
SCRAPER_TIMEOUT_MS=30000

# Maximum response size for scraped URLs (bytes)
# Default: 5242880 (5MB)
SCRAPER_MAX_SIZE_BYTES=5242880
